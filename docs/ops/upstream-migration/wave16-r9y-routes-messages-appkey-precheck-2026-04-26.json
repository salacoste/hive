{
  "generated_at_utc": "2026-04-26T01:58:05Z",
  "wave": "Wave16-R9Y",
  "title": "Routes messages AppKey manager contract lane precheck",
  "scope": [
    "core/framework/server/routes_messages.py"
  ],
  "inventory": {
    "numstat_vs_upstream_main": {
      "path": "core/framework/server/routes_messages.py",
      "add": 2,
      "del": 1,
      "total": 3
    }
  },
  "delta_summary": [
    "routes_messages imports APP_KEY_MANAGER and uses typed AppKey access instead of raw `request.app[\"manager\"]`"
  ],
  "risk_signals": [
    {
      "type": "appkey-contract-consistency",
      "evidence": "server routes standardized on APP_KEY constants for aiohttp compatibility",
      "impact": "replay to raw-string access can reintroduce contract drift"
    }
  ],
  "planned_validation": [
    "uv run --package framework pytest core/framework/server/tests/test_api.py -k 'messages_classify or classify_route' -q",
    "uv run --package framework pytest core/framework/server/tests/test_api.py -k 'messages or classify' -q"
  ],
  "decision_gate": "prefer keep_local if typed AppKey path is preserved and classify-route tests are green"
}
